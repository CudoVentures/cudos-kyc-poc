{
    "name": "@cudos-kyc-poc/server",
    "scripts": {
        "build": "tsc --build .",
        "start": "npx ts-node index.ts",
        "dev": "ts-node-dev --respawn --pretty --transpile-only index.ts"
    },
    "dependencies": {
        "@onfido/api": "^2.6.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.15",
        "ts-node-dev": "^2.0.0"
    }
}
